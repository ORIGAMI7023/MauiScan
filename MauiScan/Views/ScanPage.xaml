<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiScan.Views.ScanPage"
             Title="文档扫描">

    <Grid RowDefinitions="*,Auto" Padding="20">

        <!-- 预览区域 -->
        <Border Grid.Row="0"
                StrokeThickness="2"
                Stroke="#512BD4"
                BackgroundColor="#F5F5F5"
                Margin="0,0,0,20">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="12"/>
            </Border.StrokeShape>

            <Grid>
                <Image x:Name="PreviewImage"
                       Aspect="AspectFit"
                       IsVisible="False"/>

                <Label Text="点击下方按钮拍摄文档"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       FontSize="16"
                       TextColor="#999"
                       x:Name="PlaceholderLabel"/>

                <ActivityIndicator x:Name="LoadingIndicator"
                                   IsRunning="False"
                                   IsVisible="False"
                                   Color="#512BD4"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
            </Grid>
        </Border>

        <!-- 操作按钮 -->
        <VerticalStackLayout Grid.Row="1" Spacing="12">

            <Button x:Name="CaptureButton"
                    Text="📷 拍摄文档"
                    Clicked="OnCaptureClicked"
                    BackgroundColor="#512BD4"
                    TextColor="White"
                    HeightRequest="50"
                    CornerRadius="8"/>

            <!-- 旋转按钮 -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" x:Name="RotateButtonsGrid" IsVisible="False">
                <Button Grid.Column="0"
                        x:Name="RotateLeftButton"
                        Text="↺ 左转90°"
                        Clicked="OnRotateLeftClicked"
                        BackgroundColor="#6C757D"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="8"/>

                <Button Grid.Column="1"
                        x:Name="RotateRightButton"
                        Text="↻ 右转90°"
                        Clicked="OnRotateRightClicked"
                        BackgroundColor="#6C757D"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="8"/>
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button Grid.Column="0"
                        x:Name="SaveButton"
                        Text="💾 保存"
                        Clicked="OnSaveClicked"
                        BackgroundColor="#28A745"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="8"
                        IsEnabled="False"/>

                <Button Grid.Column="1"
                        x:Name="EnhanceButton"
                        Text="✨ 增强模式"
                        Clicked="OnEnhanceToggled"
                        BackgroundColor="#FFC107"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="8"/>
            </Grid>

            <Label x:Name="StatusLabel"
                   Text=""
                   FontSize="12"
                   TextColor="#666"
                   HorizontalOptions="Center"
                   Margin="0,8,0,0"/>
        </VerticalStackLayout>

    </Grid>

</ContentPage>
