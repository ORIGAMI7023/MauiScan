# 幕布边框检测 - 深度学习方案

## 📊 当前状态

### 传统算法测试结果（27 个样本）

| 算法 | 可接受率 | 检测成功率 | 平均误差 | 结论 |
|------|---------|-----------|---------|------|
| **Canny (baseline)** | **59.3%** | 59.3% | 28px | ✅ 当前最佳，作为保底 |
| Morphological | 22.2% | 85.2% | 833px | ❌ 过度拟合 |
| LSD + RANSAC v1 | 0% | 100% | 2529px | ❌ 完全失效 |
| LSD + RANSAC v2 | 0% | 14.8% | 1279px | ❌ 过度过滤 |

### 关键发现

1. **传统算法已达瓶颈**：Canny 的 59.3% 是手工特征的上限
2. **主要失败原因**：
   - 边框被遮挡（断裂）
   - 背景干扰（杂物、线条）
   - 低对比度（边框与背景融合）
3. **保底方案**：Canny 算法保留作为 fallback

---

## 🎯 深度学习方案目标

### 性能目标

| 指标 | 当前 (Canny) | 目标 (DL) |
|------|-------------|----------|
| 可接受率 | 59.3% | **95%+** |
| 平均误差 | 28px | **< 20px** |
| 推理时间 | ~10ms (CPU) | **< 15ms (NPU)** |
| 抗遮挡 | ❌ 极弱 | ✅ 强 |
| 抗干扰 | ❌ 弱 | ✅ 强 |

---

## 🛠️ 技术方案

### 1. 模型选型

#### 方案 A：关键点回归（推荐）
**模型**：MobileNetV3-Small + FC Head

**优势**：
- 轻量级（~2MB 模型文件）
- 直接回归 8 个数值（4 个角点 x,y）
- 训练简单，收敛快

**输出**：`[x1, y1, x2, y2, x3, y3, x4, y4]`

**部署**：ONNX → MNN/NCNN

#### 方案 B：热力图分割
**模型**：NanoDet-Plus 或 ShuffleNetV2

**优势**：
- 准确率更高
- 可视化调试方便

**劣势**：
- 模型较大（~5MB）
- 需要后处理（峰值检测）

**输出**：4 张 64×64 热力图

---

### 2. 数据增强策略

#### 2.1 几何变换
- **透视变换**：模拟不同拍摄角度
- **旋转**：±15°
- **缩放**：0.8-1.2 倍
- **翻转**：水平/垂直翻转

#### 2.2 光学增强
- **亮度调整**：±30%
- **对比度调整**：±20%
- **高斯噪声**：模拟 ISO 噪点
- **运动模糊**：模拟手抖

#### 2.3 遮挡模拟（关键）
- **随机遮挡**：在边角区域随机添加黑色方块
- **线条干扰**：添加随机线条（模拟 PPT 内容）
- **物体遮挡**：模拟人影、水杯等（使用 PNG 素材）

#### 2.4 数据量估算

| 原始样本 | 增强倍数 | 生成样本 | 训练集 | 验证集 |
|---------|---------|---------|--------|--------|
| 27 | 200 | 5,400 | 4,860 | 540 |

---

### 3. 训练流程

#### 3.1 数据准备
```python
# 数据结构
training_data/
├── images/
│   ├── 0001_aug_001.jpg
│   ├── 0001_aug_002.jpg
│   └── ...
└── labels/
    ├── 0001_aug_001.txt  # 格式: x1,y1,x2,y2,x3,y3,x4,y4
    └── ...
```

#### 3.2 模型训练
- **框架**：PyTorch（CPU 可训练，样本少）
- **损失函数**：Smooth L1 Loss / Wing Loss（对异常值鲁棒）
- **优化器**：AdamW
- **学习率**：1e-4，带 cosine annealing
- **Batch Size**：16（CPU 友好）
- **训练时长**：预计 2-4 小时（CPU）

#### 3.3 验证指标
- **IoU > 0.7** 的比例
- **平均角点误差**（像素）
- **推理时间**

---

### 4. 部署流程

#### 4.1 模型转换
```bash
# PyTorch → ONNX
torch.onnx.export(model, dummy_input, "screen_detector.onnx")

# ONNX → MNN
mnnconvert -f ONNX --modelFile screen_detector.onnx --MNNModel screen_detector.mnn
```

#### 4.2 C++ 集成
- 位置：`Native.OpenCV/screen_detector.cpp`
- 接口：`vector<Point> detectScreen(Mat image)`
- NPU 加速：MNN 推理引擎

#### 4.3 回退机制
```
用户拍照 → 尝试 DL 检测
             ↓
         成功？ → 返回结果
             ↓ 否
         尝试 Canny 检测（保底）
             ↓
         成功？ → 返回结果
             ↓ 否
         提示用户手动调整
```

---

## 📅 实施计划

### 阶段 1：数据收集（当前阶段）
- [ ] 优化 AnnotationTool 标注工具
- [ ] 标注更多真实场景数据（目标：100+ 张）
- [ ] 覆盖不同失败场景：
  - [ ] 遮挡场景（人影、物体）
  - [ ] 低对比度场景
  - [ ] 复杂背景场景
  - [ ] 不同光照条件

### 阶段 2：数据增强
- [ ] 编写数据增强脚本
- [ ] 生成 5000+ 训练样本
- [ ] 划分训练集/验证集

### 阶段 3：模型训练
- [ ] 搭建 MobileNetV3 回归模型
- [ ] CPU 训练（预计 2-4 小时）
- [ ] 验证准确率

### 阶段 4：部署集成
- [ ] 模型转换为 MNN 格式
- [ ] C++ 接口开发
- [ ] 集成到 MauiScan 应用
- [ ] 测试推理时间

### 阶段 5：测试优化
- [ ] 真机测试
- [ ] 边缘案例分析
- [ ] 性能优化

---

## 🎯 阶段性目标

| 阶段 | 目标 | 时间 |
|------|------|------|
| 数据收集 | 100+ 标注样本 | 1-2 周 |
| 数据增强 | 5000+ 训练样本 | 1-2 天 |
| 模型训练 | 95%+ 准确率 | 2-3 天 |
| 部署集成 | 集成到 App | 3-5 天 |
| **总计** | **可用功能** | **2-3 周** |

---

## 📝 备注

- **训练环境**：CPU 可训练（样本少，模型轻量）
- **部署设备**：Meizu 21 (骁龙 8 Gen 3)
- **推理框架**：MNN（阿里开源，Android 友好）
- **保底方案**：Canny 算法保留作为 fallback
- **开放考虑**：后续可考虑 SAM (Segment Anything Model) 作为更强大方案

---

## 📚 参考资料

- **MNN 文档**：https://github.com/alibaba/MNN
- **NanoDet**：https://github.com/RangiLyu/NanoDet
- **关键点检测**：https://github.com/open-mmlab/mmpose
- **数据增强**：Albumentations 库

---

*文档创建时间：2026-03-24*
*最后更新：2026-03-24*
